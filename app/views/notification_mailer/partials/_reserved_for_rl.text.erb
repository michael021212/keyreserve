下記の施設の利用予約を承りました。

<% if ksc_reservation_no.present? %>
予約コード: <%= ksc_reservation_no %>
<% end %>

施設名: <%= reservation.facility.name %>
住所: <%= reservation.facility.address %>
利用日時: <%= datetime_ja reservation.checkin %>
利用時間: <%= reservation.usage_period %>時間
人数: <%= reservation.num %>名

<%# KS Checkinを利用する場合 %>
<% if reservation.facility.rent_with_ksc? %>

当日の流れは下記となります。
※KS Check in用タブレットは開始時間30分前から操作可能となります。


1. KS Check in用タブレット設置場所への移動

利用時刻までに、下記のKS Checkin用タブレット設置箇所へお越しください。
==================================================
施設名: <%= ks_room_key_info['key_station']['name'] %>
郵便番号: 〒<%= ks_room_key_info['key_station']['postal_code'] %>
住所: <%= ks_room_key_info['key_station']['address'] %>
==================================================


2. 予約コードの入力

設置されているタブレットに、上記の予約コードを入力してください。
その後は、画面の指示に従って操作を進めてください。
操作を進めると、タブレットにリモートロック開錠用のパスワードが表示されます。


3. リモートロックの開錠
2で取得したパスワードを、リモートロックに入力し入室してください。


4. 終了時
利用が終了したら、再度KS Check in用タブレットに予約コードを入力し、「利用終了」をタッチしてください。
※こちらの作業をしていただかないと利用終了となりませんのでご注意ください。



<%# KS Checkinを利用しない場合 %>
<% else %>
<% if ks_room_key_info.present? %>
当日は、利用時刻までに、下記のキーボックス設置箇所へとお越しください。

==================================================
施設名: <%= ks_room_key_info['key_station']['name'] %>
郵便番号: 〒<%= ks_room_key_info['key_station']['postal_code'] %>
住所: <%= ks_room_key_info['key_station']['address'] %>
==================================================
<% end %>
<% end %>

何卒よろしくお願いいたします。
