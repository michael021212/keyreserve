<%= bootstrap_nested_form_for([:admin, corporation, facility, facility_temporary_plan]) do |f| %>
  <%= f.collection_select :plan_id, corporation.selectable_facility_temporary_plans, :id, :name, f.object.plan_id.present? ? { selected: f.object.plan_id } : { include_blank: true } %>
  <%= f.text_field :standard_price_per_day %>
  <%= f.text_field :standard_price_per_hour %>
  <%= f.form_group :facility_temporary_plan_prices, label: {text: "施設利用都度課金プラン時間帯価格"} do %>
    <%= f.fields_for :facility_temporary_plan_prices do |pp| %>
     <div class="form-inline mb20">
        <%= pp.time_select :starting_time, { discard_minute: true, default: { minute: '00' } }, { class: 'mr50' } %>
        <%= pp.time_select :ending_time, { discard_minute: true, default: { minute: '00' } }, { class: 'mr50' } %>
        <span class='price-text-field'>
          <%= pp.text_field :price, class: 'form-control mr50' %>
        </span>
        <%= pp.link_to_remove "削除", :confirm => t('cargo_iem.remove_item_confirmation'), class: 'glyphicon glyphicon-remove', inline: true %>
      </div>
    <% end %>
    <p><%= f.link_to_add "施設利用都度課金プラン時間帯価格を追加する", :facility_temporary_plan_prices, class: 'btn btn-info glyphicon glyphicon-plus' %></p>
  <% end %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
