<% if @user_corp.present? %>
  <% breadcrumb :edit_admin_user_corp_user, @user_corp, @user %>
<% else %>
  <% breadcrumb :edit_admin_user, @user %>
<% end %>


<div class="box box-default">
  <div class="box-header with-border">
    <h4 class="box-title"><%= User.model_name.human %>編集</h4>
  </div>
  <div class="box-body">
    <%= render 'form', user: @user, user_corp: @user_corp %>
  </div>
</div>
