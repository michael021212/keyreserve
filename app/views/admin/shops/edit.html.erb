<% breadcrumb :edit_admin_corporation_shop, @corporation, @shop %>

<div class="box box-default">
  <div class="box-header with-border">
    <h4 class="box-title"><%= Shop.model_name.human %>編集</h4>
  </div>
  <div class="box-body">
    <%= render 'form', corporation: @corporation, shop: @shop %>
  </div>
</div>
